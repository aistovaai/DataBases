Нет.
В результате второй запрос может вернуть больше строк, 
потому что в нём будут включены все строки из t1, 
в отличие от первого запроса, где строк с t1.column1 != 0 
вообще не будет в результате.
Просто там будут Null вместо значений t2.